// Configuration module
const Config = {
    // TODO: Feature A - Add theme configuration here
    // Example: theme: 'light', themeOptions: ['light', 'dark']
    
    // TODO: Feature B - Add symbol configuration here
    // Example: symbols: { player1: 'X', player2: 'O' }, customSymbols: [...]
    
    // Default game configuration
    currentPlayer: 'X',
    gameActive: true,
    boardSize: 3,
    
    // Default symbols (will be customizable in Feature B)
    playerSymbols: {
        X: 'X',
        O: 'O'
    }
};